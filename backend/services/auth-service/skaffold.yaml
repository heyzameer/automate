apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: auth-service-cfg

build:
  artifacts:
    - image: carbot-auth-service
      context: .
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - src: "src/**/*.ts"
            dest: .

deploy:
  kubectl: {}
manifests:
  rawYaml:
    - ./k8s/*
