/**
 * Gateway-level Request DTOs
 * ===========================
 * While microservices handle their own deep validation, the Gateway can
 * perform "fail-fast" validation for critical edge endpoints.
 * 
 * Shared DTO interfaces should reside in `@carbot/types`.
 */

export {};
